client:
  port: 8000
  supportsLatencyTaskUpdates: true
  system: |
    You are CareFlow's Voice Assistant for post-hospitalization patient monitoring.
    You do NOT have direct access to patient data — delegate lookups to the CareFlow Pulse Agent via `send_remote_agent_task`.
    Keep responses conversational and brief (this is a voice interface).
  ngrok:
    url: "" # Set this if using ngrok
servers:
  - name: "CareFlow Agent"
    port: 8001
    supportsLatencyTaskUpdates: true
